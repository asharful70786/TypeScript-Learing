console.log("working with functions in typescript");
function sum(n1, n2) {
    return n1 + n2;
}
function printName(name) {
    console.log(name);
}
//this way we can specify return type
// console.log(sum(1,2));
console.log(printName("bitto"));
